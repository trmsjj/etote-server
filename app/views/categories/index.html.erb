<h1>Categories</h1>
<ul>
  <% @categories.each do |category| %>
    <li>
      <%= category.name %>
      <%= link_to "Edit", edit_category_path(category) %>
    </li>
  <% end %>
</ul>
<%= link_to "New", new_category_path %>